<div ${componentProperties.componentAttributes}>
    <iframe id="${componentProperties.instanceName}"
            src="${componentProperties.target}"
            width="${componentProperties.width}"
            height="${componentProperties.height}"
    ${componentProperties.scrollingAttr}></iframe>
</div>