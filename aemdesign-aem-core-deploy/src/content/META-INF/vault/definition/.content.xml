<?xml version="1.0" encoding="UTF-8"?>
<jcr:root xmlns:sling="http://sling.apache.org/jcr/sling/1.0" xmlns:vlt="http://www.day.com/jcr/vault/1.0" xmlns:jcr="http://www.jcp.org/jcr/1.0" xmlns:nt="http://www.jcp.org/jcr/nt/1.0"
    jcr:primaryType="vlt:PackageDefinition"
    jcr:description="${description}"
    sling:resourceType="cq/packaging/components/pack/definition"
    buildCount="1"
    cqVersion="${aem.verison}"
    group="${package.group}"
    groupTitle="${package.groupTitle}"
    builtWith="${package.builtWith}"
    name="${name}"
    path="${crx.package.path}/${package.group}/${artifactId}-${version}"
    version="${version}"
    providerName="${package.providerName}"
    providerUrl="${package.providerUrl}"
    providerLink="${scm.url}"
    testedWith="${package.testedWith}">
    <thumbnail.png/>
    <thumbnail.psd/>
    <screenshots jcr:primaryType="nt:unstructured">
        <screenshot1 jcr:primaryType="nt:unstructured"/>
    </screenshots>
</jcr:root>
