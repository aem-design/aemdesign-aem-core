#!/bin/bash

echo "Deploying Monolith Package: mvn -Dvault.useProxy=false -DskipTests -e -U -P deploymentpackage clean install"
mvn -Dvault.useProxy=false -DskipTests -e -U -P deploymentpackage clean install "$@"
echo "Deployed"
